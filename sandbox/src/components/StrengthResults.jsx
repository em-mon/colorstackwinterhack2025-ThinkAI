import styles from '../modules/StrengthResults.module.css';

export default function StrengthResults({ className }) {
  return (
    <div className={`${styles.container} ${className}`}>
        <h1>HELLO</h1>
    </div>
  );
}